

export default function ScrollSpy () {
  const $sections = document.querySelectorAll("section[data-scroll-spy]");

console.log($sections)
  return (
    <div class="text-white">ScrollSpy</div>
  )
}
